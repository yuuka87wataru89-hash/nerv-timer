rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 投票結果ドキュメントのみを公開。誰でも読み取り・投票（更新）できるが、
    // ドキュメントの削除や新規コレクションの追加は許可しない簡易ルール。
    match /quiz/current {
      allow read: if true;
      allow write: if true;
    }
  }
}
